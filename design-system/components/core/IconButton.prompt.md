Icon-only button (Lucide glyph) — for search, share, row actions.

```jsx
<IconButton icon="search" label="Buscar" variant="ghost" />
```
Requires lucide.createIcons() after render. variant: ghost | outline.
